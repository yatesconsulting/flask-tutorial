@REM python -m venv venv && 
venv\Scripts\activate.bat rem (if not already, check ?how?)
rem . /var/www/.virtualenvs/flaskr-n7hAE5T6/bin/activate on linux, matches Apache changes for venv use
pip install -r requirements.txt

